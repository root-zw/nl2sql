[pytest]
testpaths = tests
python_files = test_*.py
addopts = -ra
asyncio_mode = auto
markers =
    unit: 快速单元/回归测试，不依赖外部服务
    schema: 元数据库初始化 SQL 与迁移相关测试
    integration: 需要真实服务或数据库的集成测试
norecursedirs =
    .git
    .venv
    node_modules
    frontend/dist
    docker/data
