# 施工附录与代码落点清单

## 1. 文档信息

- 状态：草案
- 类型：施工附录文档
- 最后更新时间：2026-04-17

## 2. 文档目标

本文档专门回答以下问题：

- 每个阶段真实应该改哪些代码文件
- 哪些文件应新增，哪些文件应尽量保留不动
- 每个阶段需要补哪些迁移、测试和后台页面
- 每个阶段的验收动作和回滚点分别在哪里

本文件不替代：

- `15-实施矩阵与阶段蓝图.md`
  - 它负责阶段顺序和阶段目标
- `05-共享契约与关键对象定义.md`
  - 它负责对象字段、DDL 草案和接口样例
- `14-测试迁移部署与持续交付设计.md`
  - 它负责发布门禁和 runbook

本文件只负责把这些内容继续落到真实代码路径。

## 2.1 2026-04-17 已落地快照

以下内容已按当前代码实现，不再只是目标态：

- `query_sessions`
  - 已落地到：
    - `docker/init-scripts/init_database_complete.sql`
    - `migrations/versions/20260417_0002_query_sessions.py`
    - `server/services/query_session_service.py`
    - `server/api/query/routes.py`
  - 已提供：
    - `GET /api/query-sessions/{query_id}`
    - 查询开始、选表等待、表已确定、执行确认、成功、失败、取消等关键节点写库
- `draft_actions`
  - 已落地到：
    - `docker/init-scripts/init_database_complete.sql`
    - `migrations/versions/20260417_0003_draft_actions.py`
    - `server/services/draft_action_service.py`
    - `server/models/api.py`
    - `server/api/query/routes.py`
  - 已提供：
    - `POST /api/query-sessions/{query_id}/actions`
    - 结构化动作：`confirm` / `revise` / `change_table` / `request_explanation` / `execution_decision` / `exit_current`
    - 自由输入解析为有限动作
- 统一确认前端
  - 已落地到：
    - `frontend/src/api/index.js`
    - `frontend/src/views/Chat.vue`
  - 已提供：
    - 基于 `query_sessions` 的统一确认容器
    - 确认阶段输入框自由回复走动作接口
    - 按钮确认后复用原 `query_id` 续跑查询
    - 结果态“不是这张表，重新选表”入口
- 自动选表纠错回退
  - 已落地到：
    - `server/api/query/table_selection.py`
    - `server/api/query/routes.py`
    - `server/services/draft_action_service.py`
  - 已提供：
    - 高置信度自动选表成功时保留 `candidate_snapshot`
    - `change_table` 会清空 `draft/ir/sql/result` 相关快照并回退到 `table_resolution`
    - 查询仍在运行时，会同时下发停止信号并标记 `active_queries` 为 `cancelling`
- IR 快照持久化
  - 已补齐：
    - `cache hit`
    - `explain_only`
    - `execution_guard`
    - 最终完成态

以下内容仍未落地，本文档其他部分仍应按“目标态”理解：

- `query_drafts`
- `learning_events`
- `governance_candidates`
- LangGraph / Skills Runtime / 学习编排执行器

本轮已验证测试：

- `tests/schema/test_query_sessions_schema.py`
- `tests/schema/test_draft_actions_schema.py`
- `tests/unit/query/test_query_session_service.py`
- `tests/unit/query/test_draft_action_service.py`
- `tests/unit/query/test_query_session_routes.py`
- `tests/unit/query/test_table_selection_snapshot.py`
- `frontend` 构建：`npm run build`

## 3. 当前可直接施工的事实锚点

### 3.1 后端应用与路由入口

- 应用入口：`server/main.py`
- 查询主入口：`server/api/query/routes.py`
- 流式查询入口：`WS /api/query/stream`
- 会话入口：`server/api/conversations.py`
- Dify 适配入口：`server/api/dify_adapter.py`
- 健康检查入口：`server/api/health.py`

### 3.2 查询内核主链路

当前真实主链路仍集中在以下文件：

- 表选择：`server/api/query/table_selection.py`
- NL2IR：`server/nl2ir/parser.py`
- 复杂度路由：`server/query_complexity/router.py`
- SQL 编译：`server/compiler/compiler.py`
- 成本守护：`server/exec/showplan.py`
- SQL 执行：`server/exec/executor.py`
- 流式输出：`server/api/query/stream_emitter.py`

这批文件当前应视为“业务内核”，第一波不建议整体重写。

### 3.3 当前状态与会话落点

- 会话与消息服务：`server/services/conversation_service.py`
- 技术追踪：`server/trace/tracer.py`
- 当前状态表：`conversations`、`conversation_messages`、`active_queries`
- 当前归档表：`query_history`

### 3.4 前端主入口

- 用户问数页：`frontend/src/views/Chat.vue`
- API 汇总：`frontend/src/api/index.js`
- 管理后台壳：`frontend/src/views/admin/Index.vue`
- 路由：`frontend/src/router/index.js`
- 查询日志工作台：`frontend/src/views/admin/components/QueryLogs.vue`
- 系统监控页：`frontend/src/views/admin/components/SystemMonitor.vue`

### 3.5 当前迁移、测试与部署锚点

- 初始化 SQL：`docker/init-scripts/init_database_complete.sql`
- Alembic 基线：`migrations/versions/20260414_0001_metadata_baseline.py`
- schema 测试：`tests/schema/test_init_database_sql.py`
- 基线测试：`tests/schema/test_migration_baseline.py`
- SQL 文件测试：`tests/schema/test_migration_sql_files.py`
- integration smoke：`tests/integration/test_migration_smoke.py`
- systemd：`deploy/systemd/nl2sql-backend.service`
- 基础设施：`deploy/systemd/nl2sql-infra.service`

## 4. 明确不优先重写的内核

第一波实施默认保留以下模块，除非共享契约明确要求修改它们的输入输出：

- `server/nl2ir/`
- `server/compiler/`
- `server/exec/`
- `server/api/query/table_selection.py`

第一波重构重点应放在：

- 状态外壳
- 动作协议
- 学习事实层
- 治理候选入口
- 前端交互与后台工作台的最小改造

一句话：

- 外层重构
- 内核保留

## 5. 分阶段施工落点

### 5.1 阶段 0：文档与契约收口

必须修改：

- `docs/智能化重构/00-*`
- `docs/智能化重构/05-共享契约与关键对象定义.md`
- `docs/智能化重构/15-实施矩阵与阶段蓝图.md`
- `docs/智能化重构/16-施工附录与代码落点清单.md`

完成标准：

- 第一波核心对象、第二阶段对象、北极星能力已经分开
- 阶段顺序和代码落点不再互相冲突

### 5.2 阶段 1：`query_sessions`

#### 数据与迁移

必须修改：

- `docker/init-scripts/init_database_complete.sql`
- 新增 `migrations/versions/<revision>_query_sessions.py`
- `tests/schema/test_init_database_sql.py`
- `tests/schema/test_migration_baseline.py`

建议新增：

- `tests/schema/test_query_sessions_schema.py`

#### 后端代码落点

必须修改：

- `server/api/query/routes.py`
  - 查询开始、确认等待、执行中、成功、失败、取消时统一写 `query_sessions`
- `server/services/conversation_service.py`
  - 停止继续把 `active_queries` 当作唯一状态真相
- `server/api/conversations.py`
  - 运行中查询接口与停止接口要能读到 `query_sessions`
- `server/trace/tracer.py`
  - 继续只承担技术 trace，不再承担产品状态真相

建议新增：

- `server/services/query_session_service.py`

最低可接受要求：

- 即使暂不新建 service 文件，也必须把 `query_sessions` 读写逻辑从 `routes.py` 中抽成独立函数层

#### 前端代码落点

必须修改：

- `frontend/src/views/Chat.vue`
  - 展示统一状态
- `frontend/src/api/index.js`
  - 增加查询状态读取接口

建议新增：

- `GET /api/query-sessions/{query_id}`

#### 测试落点

建议新增：

- `tests/unit/query/test_query_session_service.py`
- `tests/integration/test_query_session_flow.py`

至少要覆盖：

- 创建查询时落 `query_sessions`
- 取消时 `query_sessions` / `active_queries` / `conversation_messages` 语义一致
- 成功和失败后状态可回放

#### 验收标准

- 同一 `query_id` 在任意时刻都能查到唯一产品状态
- `conversation_messages`、`active_queries`、`QueryTracer` 不再互相抢状态真相

#### 回滚点

- 回滚新增 migration
- 关闭读取 `query_sessions` 的新逻辑，暂时回退到兼容读旧表

### 5.3 阶段 2：`draft_actions`

#### 数据与迁移

必须修改：

- `docker/init-scripts/init_database_complete.sql`
- 新增 `migrations/versions/<revision>_draft_actions.py`

按需显式化：

- `query_drafts`

#### 后端代码落点

必须修改：

- `server/api/query/routes.py`
  - 初始查询继续保留
- `server/models/api.py`
  - 如动作接口需要共用响应模型，在这里补最小模型或拆到新模型文件

建议新增：

- `server/api/query/draft_actions.py`
  - 专门承接 `POST /api/query-drafts/{draft_id}/actions`
- `server/services/draft_action_service.py`

#### 前端代码落点

必须修改：

- `frontend/src/views/Chat.vue`
  - 表选择确认、高成本确认、草稿确认统一写动作协议
- `frontend/src/api/index.js`
  - 增加动作提交接口

如果新增独立 Draft 读模型，还应修改：

- `frontend/src/router/index.js`
  - 如需新增独立草稿或详情页路由

#### 测试落点

建议新增：

- `tests/unit/query/test_draft_action_service.py`
- `tests/integration/test_draft_action_idempotency.py`

至少要覆盖：

- `idempotency_key` 去重
- 版本冲突
- 兼容 `selected_table_id(s)` / `force_execute`

#### 验收标准

- 所有确认与继续执行动作都能落成统一动作对象
- 同一 `query_id` 在多次确认后仍复用原会话和消息

#### 回滚点

- 保留旧兼容字段桥接
- 动作接口失败时可回退到旧确认参数模式

### 5.4 阶段 3：`learning_events`

#### 数据与迁移

必须修改：

- `docker/init-scripts/init_database_complete.sql`
- 新增 `migrations/versions/<revision>_learning_events.py`

按需显式化：

- `runtime_memories`

#### 后端代码落点

建议新增：

- `server/services/learning_event_service.py`
- `server/services/runtime_memory_service.py`

必须修改：

- `server/api/query/routes.py`
  - 查询完成、失败、取消、反馈入口写 `learning_events`
- `server/nl2ir/few_shot_writer.py`
  - 不再独占学习事实入口，应逐步改成消费事件或兼容写入器

异步执行建议：

- 第一版不要引入新的外部工作流平台
- 优先复用当前应用已有的后台任务模式
  - `server/api/admin/auto_sync.py`
  - `server/websocket_manager.py`
  - `server/api/query/routes.py` 中已存在的 `asyncio.create_task(...)`

更具体地说：

- 先同步写 `learning_events`
- 再用应用内后台任务消费事件
- 如确有稳定需求，再显式化 `runtime_memories`

#### 前端代码落点

必须修改：

- `frontend/src/views/Chat.vue`
  - 增加结果反馈和“记住这次偏好”入口

#### 测试落点

建议新增：

- `tests/unit/query/test_learning_event_service.py`
- `tests/integration/test_learning_event_flow.py`

至少要覆盖：

- `event_key` 去重
- 结果反馈写事件
- 取消 / 失败也能产生日志化事件

#### 验收标准

- 学习事实不再只散落在 `query_history`、`qa_few_shot_feedback`、few-shot 样本里

#### 回滚点

- 允许先停用事件消费者
- 事件表保留只读，不影响主链路继续跑

### 5.5 阶段 4：`governance_candidates`

#### 数据与迁移

必须修改：

- `docker/init-scripts/init_database_complete.sql`
- 新增 `migrations/versions/<revision>_governance_candidates.py`

按需显式化：

- `release_runs`
- `rollback_records`

#### 后端代码落点

建议新增：

- `server/services/governance_candidate_service.py`
- `server/services/publish_adapter_service.py`

必须修改：

- `server/api/admin/unified_metadata.py`
- `server/api/admin/fields.py`
- `server/api/admin/joins.py`
- `server/api/admin/rules.py`

原因：

- 发布适配器最终要回写现有正式层
- 候选审核通过后不能绕过现有治理入口

#### 前端代码落点

必须修改：

- `frontend/src/router/index.js`
- `frontend/src/views/admin/Index.vue`

建议新增：

- `frontend/src/views/admin/components/GovernanceCandidates.vue`
- `frontend/src/views/admin/components/GovernanceReview.vue`

#### 测试落点

建议新增：

- `tests/unit/governance/test_candidate_mapping.py`
- `tests/integration/test_governance_publish_flow.py`

至少要覆盖：

- 候选到正式层映射
- 正式层变更后同步链路继续工作
- 首批发布失败时可阻断

#### 验收标准

- 运行收益可以稳定进入待审层
- 审核通过后能回写现有正式表，而不是另建平行正式层

#### 回滚点

- 候选表可保留
- 发布适配器写正式层前必须有最小发布审计

### 5.6 阶段 5：`policy_snapshots`

#### 数据与迁移

按需显式化：

- `policy_snapshots`

#### 后端代码落点

必须修改：

- `server/api/query/permission_checker.py`
- `server/services/permission_injector.py`
- `server/services/permission_service.py`
- `server/api/admin/permissions.py`
- `server/api/admin/organizations.py`

#### 前端代码落点

必须修改：

- `frontend/src/views/admin/components/DataRoleManage.vue`
- `frontend/src/views/admin/components/OrganizationManage.vue`
- `frontend/src/views/admin/components/QueryLogs.vue`

#### 测试落点

- 行级过滤回放
- 导出权限回放
- 查询日志到权限审计跳转

### 5.7 阶段 6：评测、发布、回滚产品化

#### 后端代码落点

必须修改：

- `server/api/admin/monitor.py`
- `server/trace/api.py`
- `server/api/admin/history.py`

按需新增：

- `server/api/admin/release_runs.py`
- `server/api/admin/evaluation_runs.py`

#### 前端代码落点

必须修改：

- `frontend/src/views/admin/components/QueryLogs.vue`
- `frontend/src/views/admin/components/SystemMonitor.vue`

建议新增：

- 发布收益页
- 回滚记录页

### 5.8 阶段 7：平台控制面与外部适配统一化

#### 后端代码落点

必须修改：

- `server/api/admin/system_config.py`
- `server/api/admin/cache.py`
- `server/api/admin/tokenizer.py`
- `server/api/admin/websocket.py`
- `server/api/dify_adapter.py`
- `server/main.py`
- `server/websocket_manager.py`

#### 前端代码落点

必须修改：

- `frontend/src/views/admin/Index.vue`
- `frontend/src/router/index.js`

按需新增：

- 平台控制台页面

#### 测试落点

- 配置热加载
- 缓存失效
- 外部适配器不绕过状态、权限和审计

## 6. 建议新增文件骨架

如果希望第一波实现后代码结构仍然可维护，建议优先新增以下文件，而不是继续把逻辑塞回现有巨型路由：

- `server/services/query_session_service.py`
- `server/services/draft_action_service.py`
- `server/services/learning_event_service.py`
- `server/services/governance_candidate_service.py`
- `server/api/query/draft_actions.py`
- `server/api/query/query_sessions.py`
- `frontend/src/views/admin/components/GovernanceCandidates.vue`
- `tests/schema/test_query_sessions_schema.py`
- `tests/schema/test_learning_events_schema.py`
- `tests/unit/query/test_query_session_service.py`
- `tests/unit/query/test_draft_action_service.py`
- `tests/integration/test_query_state_flow.py`

如果第一阶段不想一次性加太多新文件，最低要求也应是：

- 路由层按职责拆文件
- 状态、动作、事件、候选分别进入独立 service
- 不再继续向 `server/api/query/routes.py` 集中堆逻辑

## 7. 施工前检查清单

真正开工前，先回答完以下问题：

1. 这次要落的是第一波核心对象，还是第二阶段对象。
2. 初始化 SQL、Alembic、schema 测试是否同步改。
3. 前端是否需要新页面，还是只改 `Chat.vue` / 现有后台组件。
4. 这次发布失败时是应用回滚、结构回滚还是前向修复。
5. 本次是否引入新的后台任务；如果引入，是否复用了当前应用已有的后台任务模式。

只要有一项答不上来，就还不算可直接施工。

## 8. 与其他文档的配合方式

- 阶段顺序：`15-实施矩阵与阶段蓝图.md`
- 对象字段与 DDL 草案：`05-共享契约与关键对象定义.md`
- 发布门禁与 runbook：`14-测试迁移部署与持续交付设计.md`

真正实施时建议固定顺序：

1. 先看 `15`，确认是不是当前阶段该做的事
2. 再看 `05`，确认对象和接口契约
3. 再看 `16`，按真实文件路径施工
4. 最后按 `14` 执行测试、发布和回滚动作
