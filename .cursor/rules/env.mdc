---
alwaysApply: true
---
## 1. 环境与依赖规则
- 本地开发、依赖安装、脚本执行、测试、构建相关的命令，默认在 Conda 环境 `nl2sql` 中执行。
- 如果涉及后端等环境依赖等配置时，请使用如下命令格式：
  - `conda activate nl2sql && <command>`
- 当前 systemd 运行中的后端服务环境是 `nl2sql`；查看服务状态、日志、进程时，以实际 systemd 配置为准。
- 所有自动生成的 Dockerfile / Makefile / Shell 脚本必须确保：
  - 本地开发脚本启动时激活 conda 环境 `nl2sql`
  - 使用项目提供的 requirements.txt 或 conda env 文件


## 2.通用规则
1.生成测试文件，请写入项目根目录tests中，默认使用本地开发环境 `nl2sql` 执行测试；如需核对 systemd 线上服务行为，再单独参考其实际运行环境，不要使用demo数据，生成的测试报告也要存储在tests文件夹中。
2.不要随意生成说明文档，除非明确要求
3.生成新的环境变量时，记得要及时同步到env.template文件中
4.需要执行postgesql的数据库操作，请生成脚本并直接执行，不需要我确认，如果不确认数据库连接相关的参数，请产考env.template文件数据库参数部分。
5.postgresql中fields表的is_active字段标识是人工标识。is_active true时从原始数据表中禁用部分无意义字段之后的业务字段，写入向量数据库Milvus时，无论是dense还是sparse向量，都必须只能写is_active true的元数据字段。
6.当你修改postgresql的表结构或字段时，请同步修改docker/init-scripts/init_database_complete.sql文件，实时同步本地postgresql数据库的表结构或字段。
7.前端启动使用的是`npm run dev`启动；本地后端调试使用`conda activate nl2sql && uvicorn server.main:app --host 0.0.0.0 --port 8890 --reload`启动。当你修改完代码后，我会自行重启，你无需帮我重启。若检查线上运行状态，优先查看 systemd 当前实际配置。


## 3. 文件结构与代码风格
- 新增代码需匹配现有项目结构）。
- 所有内容必须可运行（不生成伪代码）。
- TypeScript / Python 需：
  - 遵循已有 Lint 约束
  - 保持统一的命名规范（snake_case for Python / camelCase for TS）

## 4. 文档规则
- 所有解释类输出必须简洁、工程化、可落地。
- 鼓励自动补充缺失上下文，但禁止虚构不存在的字段 / 表结构。

## 5. 禁止事项
- 禁止生成未在项目中定义的字段/表名/枚举值。
- 禁止忽略 conda 环境要求。
- 禁止生成不可执行的脚本。
- 禁止硬编码，如果需要设置环境变量，直接增加到env.template，我手动拷贝到.env。
- 项目属于开发测试阶段，需要增强项目的鲁棒性和兼容性，不要采取临时或过渡方案解决问题，需要系统性解决问题。